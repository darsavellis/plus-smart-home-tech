spring:
  application:
    name: aggregator
  config:
    import: "configserver:"
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: config-server

eureka:
  instance:
    preferIpAddress: false
    hostname: AGGREGATOR
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/