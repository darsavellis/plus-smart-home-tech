spring:
  application:
    name: analyzer
  config:
    import: "configserver:"
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: config-server

eureka:
  instance:
    preferIpAddress: false
    hostname: ANALYZER
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/